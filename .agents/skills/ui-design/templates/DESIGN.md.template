# DESIGN.md — 设计系统契约

> 本文件把「感觉」翻译成**可判真假的规则**：每个属性都挂一个 token 名，
> 并写清什么情况下必须用它、什么情况下禁止绕开它。
> 描述视觉与交互，不描述产品定位（那在 PRODUCT.md）。
> **生成方式**：从项目现有 CSS 变量 / Tailwind theme / 设计资产 / 代表性组件里**反向抽取**，
> 禁止套用别的项目的数值。宁可留空标注「未定义」，也不要编一个看起来合理的值。

## 0. 视觉世界观（3–5 行）

<!-- 这个界面的基调是什么、靠什么建立身份（一个字体？一种表面？一种密度节奏？），以及刻意反对什么。
     关键：高频操作面优先可读性，品牌只活在精确细节里；表现力留给一次性的、低摩擦的接触点。 -->

## 1. Token 清单

### 1.1 色彩

| token | 值 | 用途 | 禁止 |
|---|---|---|---|
| `--bg-0` | | 页面/工作区底层 | 禁止纯黑（需带色相） |
| `--bg-panel` | | 卡片、面板表面 | |
| `--text` | | 正文与标题 | |
| `--text-muted` | | 次级文字 | 低于此灰阶禁止用于 <14px 常规重文字 |
| `--text-dim` | | 仅元数据/占位 | **禁止承载功能文字** |
| `--accent` | | 主操作/选中/进行中 | 禁止用作进度或警告 |
| `--danger` / `--warn` / `--ok` / `--info` | | 各自唯一语义 | 禁止互相借用 |

<!-- 有深色模式就分两套列出。 -->

### 1.2 排印

| token / 场景 | 字号 | 字重 | 行高 | 字距 | 备注 |
|---|---|---|---|---|---|
| 页面标题 | | | | | |
| 区块标题 | | | | | |
| 正文 | | | | | |
| UI 标签/按钮 | | 700 | | | 小字必须靠**重量**而非尺寸建立层级 |
| 辅助/元信息 | | | | | |
| 等宽（数字/路径/代码） | | | | | 表格数字需 tabular-nums |

字族：UI = ____；正文长文 = ____；等宽 = ____。**禁止混入第二个无衬线。**

### 1.3 间距与圆角

- 间距刻度：`____`（如 4/8/12/16/24/32/48）——**任何不在刻度上的值视为缺陷**
- 组内间距 vs 组间间距的比例：`____`
- 圆角阶梯：控件 `__` < 卡片 `__` < 浮层 `__`（禁止单一圆角通吃，禁止随机圆角）

### 1.4 表面（分层只靠一种手段）

| 层级 | 手段 | token |
|---|---|---|
| 卡片 | 描边 or 极轻阴影（二选一定死） | |
| 浮起（下拉/菜单/弹窗） | 玻璃配方 | `--glass-blur` = |
| 遮罩 | | |

**硬规则**：卡片禁止同时使用 边框 + 阴影 + 底色 三件；阴影必须来自既有档位。
所有毛玻璃**必须**引用同一个 blur 配方 token，禁止逐处手写 `blur(Npx)`。

### 1.5 动效

- 时长刻度：微反馈 `__ms` / 表面进出 `__ms` / 大位移 `__ms`
- 缓动：标准 `____`；进场 `____`
- 规则：位移小则时长短；同屏不叠加 >2 层同时动画；必须响应 `prefers-reduced-motion`

## 2. 组件契约

<!-- 每个高频组件一小节：结构、状态（default/hover/active/focus-visible/disabled/loading/empty/error）、
     可访问性（键盘顺序、对比度、aria）、响应式下的降级形态。 -->

### 2.1 按钮
### 2.2 卡片 / 列表行
### 2.3 弹窗（拖动+缩放、焦点陷阱、Esc）
### 2.4 表单控件（输入/选择/开关）
### 2.5 空状态 / 错误态

## 3. 禁令（Anti-patterns）

- 禁止 ad-hoc 色值：组件内出现 `rgba()`/`#hex`/`text-[11.5px]` 这类字面量即为违约（改 token）。
- 禁止「为好看而加」的装饰：高频操作面上不加渐变、光晕、粒子、纹理。
- 禁止紫蓝渐变 + 大圆角 + 等宽三卡片 的 SaaS 套路组合。
- 禁止用 emoji 充当功能图标。
- 禁止层级平铺：一屏必须能一眼指出第一焦点。
- 禁止无占位符的假内容：不写 lorem，不画图形/图标/照片（留虚线占位盒 + 精确尺寸）。

## 4. 资产

<!-- 品牌名与写法、字号上限、图片风格参考、图形语言来源；以及当前所有「占位待补」清单。 -->

## 5. 自检清单（改动前后各过一遍）

- [ ] 只用了本文件里的 token（无新增字面量）
- [ ] 第一焦点明确；层级 ≥ 3 倍差
- [ ] 同一节奏的控件共享同一间距/尺寸
- [ ] 小字对比度过 AA（肉眼在正常亮度下不吃力）
- [ ] 深色 + 浅色两套都成立
- [ ] 1440 与 900 两档宽度都不崩
- [ ] `prefers-reduced-motion` 有响应
- [ ] 截图交给独立评审模型过一轮（见 SKILL.md 阶段 5）
