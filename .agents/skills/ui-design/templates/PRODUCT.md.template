# PRODUCT.md — 产品真相

> 本文件是**设计决策的唯一事实来源**，随代码一起提交、持续演进。
> 它描述「为谁、为什么、边界在哪」，**不描述视觉风格**（那在 DESIGN.md）。
> 每次做 UI 决策前先读它；与代码冲突时以代码为准并立即更新本文件。

## 1. 这个产品是什么

<!-- 一句话说清：它是给谁用的什么东西。别写营销话术。 -->

## 2. 给谁用（受众与熟练度）

<!-- 具体到角色，不要「所有用户」。他们的专业度、每天用多久、在什么设备/环境下用。 -->
- 主要：
- 次要：

## 3. 使用场景（它在真实工作里的位置）

<!-- 用户带着什么来、要完成什么、完成后去做什么。高频还是低频？是「盯一整天」还是「每周点两次」？
     这一节直接决定 Operate（可读性优先）还是 Expression（表现力优先）。 -->

## 4. 核心对象与动作

<!-- 界面上真实存在的名词（订单、会话、表格、工单…）和用户对它们做的动词。
     视觉隐喻与图标语义应当从这里的真实对象长出来，而不是从「好看的模板」长出来。 -->

## 5. 什么绝不做（边界）

<!-- 明确排除项能防止设计漂移，例如：不做移动端、不做营销落地页、不追求炫技动效、不接受任何降低扫描效率的装饰。 -->

## 6. 成功长什么样（可验证）

<!-- 不是「好看」，而是「新用户 30 秒内找到 X」「一屏能扫完 12 行而无需横向滚动」这类可判真假的句子。 -->

## 7. 平台与运行环境

<!-- 桌面 web / Electron 壳 / 移动原生。是否尊重 prefers-reduced-transparency、有无深色模式、目标窗口尺寸区间。 -->

---

## 8. 语气 voice（可选，有对话式界面才填）

| 维度 | 取值 |
|---|---|
| 正式 ↔ 随意 | |
| 简洁 ↔ 详尽 | |
| 克制 ↔ 热情 | |
| 平实 ↔ 活泼 | |
| 硬规则（禁用词 / 禁用标点 / 禁用句式） | |
