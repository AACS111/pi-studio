# 2026-08-06

## 项目解析（首次完整梳理 pi-web）

- 项目定位：pi 编程智能体（@earendil-works/pi-* 0.83.0）的本地 Web UI，数据源为 ~/.pi/agent/sessions/*.jsonl，本地优先无远程依赖。
- 技术栈：Next.js 16.2.12 + React 19 + Tailwind 4 + TS 5；dev 端口 10141（127.0.0.1），start 端口 30141。
- 架构：浏览器 React 组件层 → Next.js API 路由层（约 15 组）→ 进程内 AgentSession（rpc-manager.ts 的 startRpcSession）→ 磁盘 jsonl。
- 关键机制：globalThis.__piSessions 注册表扛热重载；fork 后必须 destroy wrapper；in-session branch 用 navigate_tree + leafId 切换；normalize.ts 归一化 toolCall 字段；开发期禁用 next build。
- 代码库较 README 增量：file-index、git、project-trust、plugins、i18n、PWA（PwaRegistration/MobilePwaLayout）、custom-ui-terminal。
- 测试：40+ 个 *.test.mjs 单测（lib/ 下与组件测试并存）。
