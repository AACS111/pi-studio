# PRODUCT.md — Pi Studio 产品真相

> 设计决策的唯一事实来源：**为谁、为什么、边界在哪**。不描述视觉风格（那在 `DESIGN.md`）。
> 内容取自 `AGENTS.md` / `docs/agents/*` / 代码里的既有约定；**标注「推断」的条目需要用户校准**。
> 生成方式：`.agents/skills/ui-design/` 阶段 0（2026-09-19）。

## 1. 这个产品是什么

Pi Studio 是 **pi coding agent 的图形界面**：把原本只有终端的 agent 会话，做成一块
可以长时间驻留的工作台——看历史会话、发消息、看工具调用与产物、管理模型与 skill、
在右侧直接查看/编辑表格与网页，并以 Electron 桌面应用形态分发给他人使用。

两种运行形态共用同一套 UI 与请求链路（`docs/agents/architecture.md`）：浏览器模式与
Electron 桌面模式（原生窗口 + 右侧浏览器控制桥）。**桌面形态是主形态**。

## 2. 给谁用

- **主要**：本人——同时在多个项目上驱动 agent、频繁切换模型、把表格/文档/网页当第二工作台的人。
  专业度高、容忍密度、讨厌为好看而牺牲效率。
- **次要**：拿到安装包的其他用户（`pack:nsis` / 便携版）。**推断**：其中包含不写代码的人
  ——他们把 Pi Studio 当「和 AI 协作的桌面应用」，而不是 IDE。
  → 含义：首次使用路径（选项目、选模型、发第一条消息）必须能自己走通，不能依赖命令行知识。
- 熟练度差异真实存在：同一屏里既有「看得懂 tool 输出的人」，也有「只想对话的人」。

## 3. 使用场景

- **长驻**：一天开着，切来切去。界面必须经得起反复看，装饰会快速变成噪声。
- **多任务并行**：多个会话、多个项目（cwd）、多个模型；侧栏是导航主动脉。
- **主要动作**：读会话 → 发下一条消息 → 看 agent 跑工具 → 打开它产出的文件/表格/网页 → 自己上手改一点。
- **窗口尺寸**：桌面大窗为主，但右侧面板可拖窄、窗口会被拉小或分屏。
- 判定：**这是 Operate 型界面**（高频操作工作台），不是 Expression 型（展示页）。
  可读性与扫描效率压倒表现力；品牌只活在精确细节里。

## 4. 核心对象与动作

| 对象 | 用户要对它做什么 |
|---|---|
| 会话（session）/ 消息 / 工具调用 | 新建、切换、继续、回溯、看 diff、删除（含批量清理） |
| 项目（cwd）/ worktree | 信任、切换、比较、合并/丢弃（agent **永不自动合并**） |
| 模型（提供商 / enabledModels / 附属模型） | 选择、测试、配置视觉与描述模型 |
| skill | 安装、更新、锁定（可编辑副本被用户改动后**永不覆盖**） |
| 表格与文档（`.univer` / `.xlsx` / `.docx` / `.pptx`） | 在右侧打开、就地编辑、写回原件、导出 |
| 网页（dev server / 在线文档） | 推到右侧面板预览、由 agent 控制与诊断（仅 Electron） |
| 项目组（team / 多 agent） | 建团队、派活、看流转、审批（该功能当前暂缓） |

**结构隐喻**：左侧「我的会话/项目」= 书架；中间 = 正在发生的工作；右侧 = 手里那份东西。
三块是**不同职责**，不是三个等宽面板。

## 5. 什么绝不做

- 不做营销落地页式界面：无 hero、无「三张功能卡」、无介绍性插画。
- 不在高频操作面加装饰（渐变、光晕、粒子、纹理、玻璃效果仅用于浮起层与桌面壳）。
- 不引入 Web Font 依赖（系统字族栈；见 `DESIGN.md` §1.3）。
- 不做移动端 App，但**必须**在窄窗口 / 分屏下不崩。
- 不做固定尺寸、不可拖动不可缩放的弹窗（统一 `DraggableResizableModal`）。
- 不为美观牺牲 agent 主流程性能（流式输出、会话列表滚动不能卡）。
- 不做只有中文或只有英文的界面（i18n en / zh-CN 全覆盖）。

## 6. 成功长什么样（可验证）

- 新用户不读文档能完成：信任项目 → 选模型 → 发出第一条消息。
- 一屏扫得完当前项目的所有会话，且能立刻认出「哪个在跑、哪个等我回复」。
- agent 产出的表格/网页，一次点击就在右侧打开并可编辑，不需要离开窗口。
- UI 改动后：浅色/深色都成立、1440 与 900 两档宽度都不崩、小字对比度达标。
- 长时间使用后仍觉得安静、不刺眼——**这条比「第一眼惊艳」更重要**。

## 7. 平台与运行环境

- Electron 桌面（透明窗口 + 桌面玻璃，`prefers-reduced-transparency` 会关掉缝隙与模糊）；
  浏览器模式次之（PWA，无右侧浏览器桥）。
- 深浅两套主题必须同时成立；accent 允许用户自定义。
- 键盘可达：常用动作有快捷键，`focus-visible` 不可丢。
- 打包链路：`pack:dir` / `pack:nsis` / 便携版；`.agents/skills` 随包分发并在启动时同步到
  `~/.agents/skills`（见 `docs/agents/design-system.md` §3）。

## 8. 语气 voice

| 维度 | 取值 |
|---|---|
| 正式 ↔ 随意 | 偏正式、工程化，不卖萌 |
| 简洁 ↔ 详尽 | 极简；标签 ≤ 6 个汉字，长解释收进 tooltip |
| 克制 ↔ 热情 | 克制；无感叹号，无「太棒了！」 |
| 硬规则 | 中英混排时术语保留英文（session、worktree、skill、token）；不出现「亲」类称呼；错误信息必须说清**下一步做什么**，不许只说「失败」 |

---

**待校准**：§2 的「次要受众」与 §6 的新用户自助路径是推断；若目标用户其实只有开发者本人，
§2/§6 可收紧，首次使用路径的设计权重就可以降低。
